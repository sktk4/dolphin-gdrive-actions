Service menu templates live in `src/dolphin_gdrive_actions/data/servicemenus/`.

Run `dga setup` to install them into `~/.local/share/kio/servicemenus/`.

Menu ordering constraints (KDE TopLevel submenu vs flat actions) are documented in [AGENTS.md](../../AGENTS.md).
